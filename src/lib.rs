//! Scaffolded by `kap new`. Replace with your crate; the loop builds the features from structure.md.
pub fn ready() -> bool {
    true
}

#[cfg(test)]
mod tests {
    #[test]
    fn ready() {
        assert!(super::ready());
    }
}
